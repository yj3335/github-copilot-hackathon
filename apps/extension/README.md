# Extension Shell

This folder contains a browser-extension starter that aligns with the requirements document:

- popup entry point with quick actions
- workspace page for local PDF loading and staged operations
- settings page for persistent preferences
- strict manifest defaults with minimal permissions

The current implementation is a static shell meant to be packaged by browser-specific build steps added in CI.
